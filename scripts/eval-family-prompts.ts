/**
 * Level 1 — Prompt inspection script for family composition logic.
 *
 * Run: npm run eval:family
 *
 * Tests buildCompositionSegment with 7 family scenarios, prints each
 * prompt output, and runs assertions.
 *
 * PHI-118: thin wrapper. Suite logic lives in `lib/evals/family/`.
 */

import { main } from "../lib/evals/family/runner";

main();
