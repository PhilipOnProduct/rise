/**
 * PHI-90 — Itinerary anchors eval
 *
 * Run: npm run eval:anchors (requires dev server on localhost:3000)
 *
 * PHI-118: thin wrapper. Suite logic lives in `lib/evals/anchors/`.
 */

import { main } from "../lib/evals/anchors/runner";

main();
